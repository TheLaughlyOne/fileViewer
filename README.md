Run the application with viewer.jar.
viewer.java contains the source code.

It is a TERRIBLE idea to use this for scanning large folders or viewing large files, this was written in 2 days on a whim and filled with inefficiencies, not to mention Java has a lot of overhead already.
